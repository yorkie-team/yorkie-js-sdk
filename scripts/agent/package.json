{
  "name": "@yorkie/agent-scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Agent command-surface scripts (review panel orchestrator). Vendored from yorkie-team/yorkie; not part of the pnpm workspace.",
  "scripts": {
    "test": "node --test-timeout=60000 --test '**/*.test.mjs'"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.217",
    "zod": "4.4.3"
  },
  "comment": "This project has its own npm lockfile and sits outside the pnpm workspace; nothing in the SDK build reaches it. See README.md in this directory. Transitive security pins are duplicated here on purpose.",
  "overrides": {
    "fast-uri": "^3.1.6",
    "qs": "^6.16.0"
  }
}
